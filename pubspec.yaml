name: dart_balancer
description: Text balancing widget with ratio control, RichText support, and Korean/CJK keepAll. Inspired by react-wrap-balancer.
version: 0.1.0
homepage: https://github.com/your-username/dart_balancer
repository: https://github.com/your-username/dart_balancer
issue_tracker: https://github.com/your-username/dart_balancer/issues

environment:
  sdk: ^3.0.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: false
